import 'package:flutter/material.dart';

const COLOR_PRIMARY = Color(0xFF01c8c9);
const COLOR_PRIMARY_LIGHT = Color(0xFF63fcfc);
const COLOR_PRIMARY_DARK = Color(0xFF009798);
const COLOR_TEXT = Color(0xFF000000);

const AGREEMENT_STATUS_PENDING = 0;
const AGREEMENT_STATUS_ACCEPTED = 1;
const KEY_AGREEMENT_STATUS = 'agreement_status';

const PAGE_STATUS_LOADING = 0;
